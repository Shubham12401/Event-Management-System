Create backend folder and add server.js file.
Install the dependencies in backend npm install express nodemailer body-parser cors
For frontend use The live server or open the file index.js in browser
